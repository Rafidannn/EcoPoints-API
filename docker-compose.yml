services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ecopoints-api
    restart: on-failure:3
    ports:
      - "${APP_PORT:-8090}:${APP_PORT:-8090}"
    env_file:
      - .env
    environment:
      - APP_ENV=production
